{
  "name": "screenstream-access-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome Extension build for ScreenStream-Access",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "dependencies": {
    "livekit-client": "^2.9.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
